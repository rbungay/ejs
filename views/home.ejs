<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= name %></title>
  </head>
  <body>
    <%- include('./partials/nav.ejs') %>
    <h1><%= name %></h1>
    <p>
      <%= address %> <br />
      <%= phone %> <br />
      <% let isOpen = true %> <% if (isOpen) { %> Yes we are open! <% } else{ %>
      Sorry we are closed. <% } %>
    </p>
  </body>
</html>
